# -*- shell-script -*-
# mksh rc file

alias ls='gls --color=auto -FA'

HOSTNAME=${HOSTNAME:=$(hostname)}

PS1='${USER:=$(id -un)}'"@${HOSTNAME%%.*}:\${PWD/#\$HOME/~} $(
        if (( USER_ID )); then print \$; else print \#; fi) "
