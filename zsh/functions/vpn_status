# check if the Junos VPN is up
local vpn_status
vpn_status=$(ifconfig | egrep "10\..* --> 10\.")

if [[ $vpn_status != "" ]]; then
    echo "VPN is UP: $vpn_status"
    return 0
else
    echo "VPN is DOWN"
    return 200
fi
