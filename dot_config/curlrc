# curl configuration

# show error messages
# show-error

# include HTTP headers in the output
# include

# follow redirects (-L)
# location

# When following a redirect, automatically set the previous URL as referer.
# referer = ";auto"

# enable verbose output: will show all headers and commands sent by curl to the # remote server
# verbose
